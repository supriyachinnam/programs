#include <stdio.h>

int main()
{
    char str1[100];
    int i;
    printf("Enter a string:");
    gets(str1);
    for(i=0;str1[i]!='\0';i++)
    {
        if(i%2!=0)
        {
            str1[i]=str1[i]-32;
        }

    }
    puts(str1);
    return 0;
}
