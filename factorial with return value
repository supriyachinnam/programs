#include <stdio.h>
void main() 
{
    int x,n,result;
    printf("Enter a number:");
    scanf("%d",&n);
    result=fact(n);
    printf("%d",result);
}
 int fact(int x)
 {
     int i,fact=1;
     for(i=1;i<=x;i++)
     {
         fact=fact*i;
     }
     return(fact);
}
