#include<stdio.h>
int main()
{
   char str[30];
   int i;
   printf("Enter a string:");
   gets(str);
   for(i=0;str[i]!='\0';i++)
   {
            if(str[i]=='a')
             {
                 str[i]='A';
             }
   }
   printf("\n");
   puts(str);
}
